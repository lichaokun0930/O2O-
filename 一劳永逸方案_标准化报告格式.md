# 一劳永逸方案：标准化报告格式

## 📋 方案概述

**目标**：从源头彻底解决硬编码索引问题，确保报告生成和读取的完全一致性

**核心思想**：在报告生成时就确保数据结构标准化，Dashboard只需按标准读取

---

## 🎯 两步走战略

### ✅ 第1步：SmartColumnFinder（已完成）

**时间**：今天
**状态**：✅ 已实施
**文件**：`dashboard_v2.py` 第535-650行

**功能**：
- 三层查找机制（精确匹配 → 关键词匹配 → 索引备用）
- 支持多种列名变体
- 详细日志记录
- 兼容历史报告

**优势**：
- ✅ 立即可用，无需修改报告生成脚本
- ✅ 兼容所有历史报告
- ✅ 容错性强，支持多种格式

---

### 🚀 第2步：标准化报告格式（长期方案）

**时间**：本周开始，逐步迁移
**目标**：从源头确保数据结构标准化

#### 2.1 创建报告标准规范

创建 `report_schema.py`：

```python
"""
报告数据标准规范
定义所有报告的标准结构，确保生成和读取的一致性
"""

class ReportSchema:
    """报告标准规范 - 数据契约（Data Contract）"""
    
    # ==================== 标准工作表名称 ====================
    SHEET_NAMES = {
        'kpi': '核心指标对比',
        'category_l1': '美团一级分类详细指标',
        'category_l3': '美团三级分类详细指标',
        'price': '价格带分析',
        'role': '商品角色分析',
        'cost': '成本分析汇总',
        'metadata': '元数据'  # 新增：元数据工作表
    }
    
    # ==================== KPI指标标准列名 ====================
    KPI_COLUMNS = {
        # 基础信息
        'store_name': '门店名称',
        
        # SKU数量指标
        'total_sku_with_spec': '总SKU数(含规格)',
        'total_sku_dedup': '总SKU数(去重后)',
        'single_spec_sku': '单规格SKU数',
        'multi_spec_sku': '多规格SKU总数',
        'unique_multi_spec': '唯一多规格商品数',
        
        # 动销指标
        'active_sku': '动销SKU数',
        'inactive_sku': '滞销SKU数',
        'active_rate': '动销率',
        
        # 销售指标
        'total_revenue': '总销售额(去重后)',
        'burst_products': '门店爆品数',
        'burst_concentration': '爆款集中度',
        'avg_sku_price': '平均SKU单价',
        
        # 价格与促销指标
        'high_value_ratio': '高价值SKU占比',
        'avg_discount': '门店平均折扣',
        'promotion_intensity': '促销强度',
        
        # 成本与毛利指标
        'total_cost': '总成本销售额',
        'total_profit': '总毛利',
        'avg_margin': '平均毛利率',
        'high_margin_count': '高毛利商品数'
    }
    
    # ==================== 分类分析标准列名 ====================
    CATEGORY_COLUMNS = {
        'category_name': '一级分类',
        'sku_count': 'SKU数',
        'active_sku': '动销SKU数',
        'active_rate': '动销率',
        'revenue': '销售额',
        'burst_count': '爆品数',
        'discount': '折扣',
        'margin': '毛利率'
    }
    
    # ==================== 价格带分析标准列名 ====================
    PRICE_COLUMNS = {
        'price_range': '价格带',
        'sku_count': 'SKU数量',
        'revenue': '销售额',
        'revenue_ratio': '销售额占比',
        'sku_ratio': 'SKU占比'
    }
    
    # ==================== 商品角色分析标准列名 ====================
    ROLE_COLUMNS = {
        'role': '商品角色',
        'sku_count': 'SKU数量',
        'revenue': '销售额',
        'revenue_ratio': '销售额占比',
        'sku_ratio': 'SKU占比'
    }
    
    # ==================== 元数据结构 ====================
    METADATA_COLUMNS = {
        'field_name': '字段名',
        'sheet_name': '工作表',
        'column_name': '列名',
        'data_type': '数据类型',
        'description': '说明'
    }
    
    @staticmethod
    def get_kpi_column_name(field_key):
        """获取KPI字段的标准列名"""
        return ReportSchema.KPI_COLUMNS.get(field_key)
    
    @staticmethod
    def get_category_column_name(field_key):
        """获取分类字段的标准列名"""
        return ReportSchema.CATEGORY_COLUMNS.get(field_key)
    
    @staticmethod
    def validate_report(excel_path):
        """验证报告是否符合标准
        
        Args:
            excel_path: Excel报告路径
            
        Returns:
            (is_valid, errors): 验证结果和错误列表
        """
        import pandas as pd
        errors = []
        
        try:
            # 检查工作表是否存在
            xl = pd.ExcelFile(excel_path)
            required_sheets = ['核心指标对比', '美团一级分类详细指标']
            for sheet in required_sheets:
                if sheet not in xl.sheet_names:
                    errors.append(f"缺少必需工作表: {sheet}")
            
            # 检查KPI列名
            if '核心指标对比' in xl.sheet_names:
                kpi_df = pd.read_excel(excel_path, sheet_name='核心指标对比')
                required_kpi_cols = ['总SKU数(含规格)', '动销率', '总销售额(去重后)']
                for col in required_kpi_cols:
                    if col not in kpi_df.columns:
                        errors.append(f"KPI工作表缺少列: {col}")
            
            # 检查分类列名
            if '美团一级分类详细指标' in xl.sheet_names:
                cat_df = pd.read_excel(excel_path, sheet_name='美团一级分类详细指标')
                required_cat_cols = ['一级分类', 'SKU数', '动销率']
                for col in required_cat_cols:
                    if col not in cat_df.columns:
                        errors.append(f"分类工作表缺少列: {col}")
            
            return len(errors) == 0, errors
        
        except Exception as e:
            errors.append(f"验证失败: {str(e)}")
            return False, errors
    
    @staticmethod
    def generate_metadata(excel_path):
        """生成报告元数据
        
        Args:
            excel_path: Excel报告路径
            
        Returns:
            DataFrame: 元数据表
        """
        import pandas as pd
        
        metadata_rows = []
        xl = pd.ExcelFile(excel_path)
        
        for sheet_name in xl.sheet_names:
            df = pd.read_excel(excel_path, sheet_name=sheet_name)
            for col in df.columns:
                metadata_rows.append({
                    '字段名': col,
                    '工作表': sheet_name,
                    '列名': col,
                    '数据类型': str(df[col].dtype),
                    '说明': ''
                })
        
        return pd.DataFrame(metadata_rows)
```

#### 2.2 修改报告生成脚本

修改 `untitled1.py` 中的 `export_full_report_to_excel` 函数：

```python
def export_full_report_to_excel(all_results, all_store_data, output_filename):
    """将所有分析结果和详细报告导出到Excel（标准化版本）"""
    
    # 导入标准规范
    from report_schema import ReportSchema
    
    # ... 现有代码 ...
    
    with writer:
        # 1. 生成KPI工作表（使用标准列名）
        kpi_df = pd.DataFrame({
            ReportSchema.KPI_COLUMNS['store_name']: [store_name],
            ReportSchema.KPI_COLUMNS['total_sku_with_spec']: [total_sku],
            ReportSchema.KPI_COLUMNS['total_sku_dedup']: [dedup_sku],
            ReportSchema.KPI_COLUMNS['active_sku']: [active_sku],
            ReportSchema.KPI_COLUMNS['active_rate']: [active_rate],
            ReportSchema.KPI_COLUMNS['total_revenue']: [total_revenue],
            ReportSchema.KPI_COLUMNS['avg_margin']: [avg_margin],
            # ... 其他字段
        })
        kpi_df.to_excel(writer, sheet_name=ReportSchema.SHEET_NAMES['kpi'], index=False)
        
        # 2. 生成分类工作表（使用标准列名）
        category_df = pd.DataFrame({
            ReportSchema.CATEGORY_COLUMNS['category_name']: categories,
            ReportSchema.CATEGORY_COLUMNS['sku_count']: sku_counts,
            ReportSchema.CATEGORY_COLUMNS['active_rate']: active_rates,
            ReportSchema.CATEGORY_COLUMNS['revenue']: revenues,
            ReportSchema.CATEGORY_COLUMNS['burst_count']: burst_counts,
            ReportSchema.CATEGORY_COLUMNS['discount']: discounts,
            # ... 其他字段
        })
        category_df.to_excel(writer, sheet_name=ReportSchema.SHEET_NAMES['category_l1'], index=False)
        
        # 3. 生成元数据工作表（新增）
        metadata_df = ReportSchema.generate_metadata(output_filename)
        metadata_df.to_excel(writer, sheet_name=ReportSchema.SHEET_NAMES['metadata'], index=False)
        
        # ... 其他工作表 ...
    
    # 验证报告
    is_valid, errors = ReportSchema.validate_report(output_filename)
    if not is_valid:
        print(f"⚠️ 报告验证失败:")
        for error in errors:
            print(f"   - {error}")
    else:
        print(f"✅ 报告验证通过，符合标准规范")
```

#### 2.3 修改Dashboard读取逻辑

修改 `dashboard_v2.py` 中的 `DataLoader` 类：

```python
class DataLoader:
    """数据加载器 - 支持标准化报告和旧格式报告"""
    
    def __init__(self, excel_path):
        self.excel_path = excel_path
        self.data = {}
        self.is_standard_format = False  # 是否为标准格式
        
        # 检查是否为标准格式
        self._check_format()
        
        # 加载数据
        self._load_data()
    
    def _check_format(self):
        """检查报告格式"""
        try:
            xl = pd.ExcelFile(self.excel_path)
            # 如果包含元数据工作表，则为标准格式
            if '元数据' in xl.sheet_names:
                self.is_standard_format = True
                logger.info("✅ 检测到标准格式报告")
            else:
                self.is_standard_format = False
                logger.info("⚠️ 检测到旧格式报告，使用SmartColumnFinder兼容")
        except Exception as e:
            logger.error(f"❌ 检查报告格式失败: {e}")
            self.is_standard_format = False
    
    def get_kpi_summary(self):
        """获取KPI摘要（支持标准格式和旧格式）"""
        summary = {}
        
        if self.is_standard_format:
            # 标准格式：直接按列名读取
            kpi_df = self.data['kpi']
            for field_key, col_name in ReportSchema.KPI_COLUMNS.items():
                if col_name in kpi_df.columns:
                    summary[col_name] = kpi_df[col_name].iloc[0]
            
            # 从分类表获取爆品数和折扣
            if not self.data['category_l1'].empty:
                category_df = self.data['category_l1']
                burst_col = ReportSchema.CATEGORY_COLUMNS['burst_count']
                discount_col = ReportSchema.CATEGORY_COLUMNS['discount']
                
                if burst_col in category_df.columns:
                    summary['门店爆品数'] = category_df[burst_col].sum()
                if discount_col in category_df.columns:
                    summary['门店平均折扣'] = category_df[discount_col].mean()
        else:
            # 旧格式：使用SmartColumnFinder
            # ... 现有逻辑（使用SmartColumnFinder）...
            pass
        
        return summary
```

---

## 📊 实施计划

### 阶段1：立即实施（今天）✅ 已完成

- [x] 创建SmartColumnFinder类
- [x] 重构get_kpi_summary方法
- [x] 测试验证

### 阶段2：创建标准规范（本周）

- [ ] 创建report_schema.py
- [ ] 定义所有标准列名
- [ ] 实现验证函数
- [ ] 实现元数据生成函数

### 阶段3：修改报告生成（下周）

- [ ] 修改untitled1.py
- [ ] 使用ReportSchema生成报告
- [ ] 添加元数据工作表
- [ ] 添加报告验证

### 阶段4：修改Dashboard读取（下周）

- [ ] 修改DataLoader类
- [ ] 支持标准格式和旧格式
- [ ] 自动检测报告格式
- [ ] 优先使用标准格式

### 阶段5：逐步迁移（未来）

- [ ] 新报告使用标准格式
- [ ] 旧报告继续兼容
- [ ] 逐步移除SmartColumnFinder的第3层（索引备用）
- [ ] 最终只使用列名查找

---

## 🎯 最终状态

### 报告生成端

```python
# 使用标准列名生成报告
kpi_df = pd.DataFrame({
    '门店名称': [store_name],
    '总SKU数(含规格)': [total_sku],
    '动销率': [active_rate],
    '总销售额(去重后)': [total_revenue],
    # ... 所有字段都使用标准列名
})
```

### Dashboard读取端

```python
# 直接按标准列名读取
summary['总SKU数(含规格)'] = kpi_df['总SKU数(含规格)'].iloc[0]
summary['动销率'] = kpi_df['动销率'].iloc[0]
summary['总销售额(去重后)'] = kpi_df['总销售额(去重后)'].iloc[0]
# ... 不再需要SmartColumnFinder
```

### 元数据工作表

```
Sheet: 元数据
┌──────────────┬──────────────────────┬────────────────┬──────────┬────────┐
│ 字段名       │ 工作表               │ 列名           │ 数据类型 │ 说明   │
├──────────────┼──────────────────────┼────────────────┼──────────┼────────┤
│ 门店爆品数   │ 美团一级分类详细指标 │ 爆品数         │ int64    │ ...    │
│ 门店平均折扣 │ 美团一级分类详细指标 │ 折扣           │ float64  │ ...    │
│ 总销售额     │ 核心指标对比         │ 总销售额(去重后)│ float64  │ ...    │
└──────────────┴──────────────────────┴────────────────┴──────────┴────────┘
```

---

## ✅ 优势总结

### 短期（SmartColumnFinder）

- ✅ 立即可用
- ✅ 兼容所有历史报告
- ✅ 容错性强
- ✅ 无需修改报告生成脚本

### 长期（标准化报告）

- ✅ 从源头解决问题
- ✅ 完全消除硬编码
- ✅ 自描述（元数据工作表）
- ✅ 易于维护和扩展
- ✅ 支持报告验证
- ✅ 一劳永逸

---

## 🤔 关键决策

### 1. 何时开始标准化？

**建议**：本周开始创建report_schema.py，下周开始修改报告生成脚本

### 2. 如何处理历史报告？

**建议**：保持兼容，Dashboard自动检测格式（标准格式优先，旧格式使用SmartColumnFinder）

### 3. 何时移除SmartColumnFinder？

**建议**：当所有报告都标准化后（预计3-6个月），逐步移除第3层（索引备用），最终只保留第1层（精确匹配）

---

## 📝 总结

这个两步走战略确保了：

1. **短期**：立即解决当前问题（SmartColumnFinder）
2. **长期**：从源头彻底解决问题（标准化报告）
3. **兼容性**：新旧格式都能正常工作
4. **可维护性**：集中管理，易于扩展
5. **一劳永逸**：最终完全消除硬编码

你觉得这个方案如何？我们可以立即开始阶段2（创建report_schema.py）吗？
