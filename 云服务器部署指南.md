# ☁️ Dashboard 云服务器部署指南

## 🎯 推荐方案：腾讯云轻量应用服务器

### 一、购买服务器（约10分钟）

1. **访问腾讯云官网**
   - 地址：https://cloud.tencent.com/
   - 搜索"轻量应用服务器"

2. **选择配置**
   - CPU: 2核
   - 内存: 2GB
   - 带宽: 3Mbps
   - 系统: Ubuntu 20.04 LTS
   - 地域: 选择离你最近的（如北京/上海）
   - **价格**: 约¥74/年（新用户有优惠）

3. **购买后获取**
   - 公网IP：例如 `123.45.67.89`
   - 登录密码（设置或重置）

---

## 二、上传文件到服务器（2种方法）

### 方法A：使用WinSCP（推荐，图形界面）

1. **下载WinSCP**
   - 官网：https://winscp.net/
   - 免费软件

2. **连接服务器**
   ```
   主机名: 你的服务器IP
   端口: 22
   用户名: root
   密码: 你设置的密码
   ```

3. **上传文件**
   - 左侧：本地文件夹
   - 右侧：服务器 `/root/dashboard/`
   - 拖拽上传以下文件：
     * dashboard_v2.py
     * reports/ 文件夹
     * requirements.txt（见下文）

### 方法B：使用命令行（适合熟悉命令行的用户）

在本地PowerShell执行：
```powershell
# 压缩文件
Compress-Archive -Path "dashboard_v2.py","reports" -DestinationPath dashboard.zip

# 上传到服务器（需要scp命令，Win10自带）
scp dashboard.zip root@你的服务器IP:/root/
```

---

## 三、安装环境（约5分钟）

SSH连接到服务器后执行：

```bash
# 1. 更新系统
apt update && apt upgrade -y

# 2. 安装Python 3.9
apt install python3.9 python3.9-pip -y

# 3. 创建工作目录
mkdir -p /root/dashboard
cd /root/dashboard

# 4. 安装依赖
pip3 install dash plotly pandas openpyxl dash-bootstrap-components
```

---

## 四、部署Dashboard（约3分钟）

### 1. 创建启动脚本

创建文件 `/root/dashboard/start.sh`：
```bash
#!/bin/bash
cd /root/dashboard
nohup python3 dashboard_v2.py > dashboard.log 2>&1 &
echo "Dashboard 已启动，访问: http://你的服务器IP:8050"
```

赋予执行权限：
```bash
chmod +x start.sh
```

### 2. 启动Dashboard
```bash
./start.sh
```

### 3. 设置开机自启动

创建文件 `/etc/systemd/system/dashboard.service`：
```ini
[Unit]
Description=O2O Dashboard Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/dashboard
ExecStart=/usr/bin/python3 /root/dashboard/dashboard_v2.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启用服务：
```bash
systemctl enable dashboard
systemctl start dashboard
systemctl status dashboard  # 检查状态
```

---

## 五、配置防火墙和安全组

### 1. 服务器防火墙
```bash
# Ubuntu防火墙
ufw allow 8050/tcp
ufw enable
```

### 2. 云服务器安全组（重要！）

在腾讯云控制台：
1. 进入"轻量应用服务器"
2. 点击实例 → "防火墙"
3. 添加规则：
   - 类型: 自定义TCP
   - 端口: 8050
   - 来源: 0.0.0.0/0（所有IP）
   - 策略: 允许

---

## 六、访问Dashboard

### 浏览器访问
```
http://你的服务器IP:8050
```

例如：`http://123.45.67.89:8050`

### 绑定域名（可选）

如果有域名，可以配置：
1. 域名解析 A记录指向服务器IP
2. 安装Nginx反向代理
3. 配置SSL证书（免费Let's Encrypt）

---

## 七、日常管理

### 查看日志
```bash
tail -f /root/dashboard/dashboard.log
```

### 重启服务
```bash
systemctl restart dashboard
```

### 停止服务
```bash
systemctl stop dashboard
```

### 更新Dashboard
```bash
cd /root/dashboard
# 上传新的dashboard_v2.py
systemctl restart dashboard
```

---

## 八、成本对比

| 方案 | 月成本 | 年成本 | 稳定性 | 速度 |
|------|--------|--------|--------|------|
| 花生壳免费版 | ¥0 | ¥0 | ⭐⭐ | ⭐⭐ |
| 花生壳付费版 | ¥6 | ¥72 | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 腾讯云轻量服务器 | ¥6 | ¥74 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**推荐**：腾讯云轻量服务器，价格几乎一样，但更稳定、更快、更专业！

---

## 九、故障排查

### Dashboard无法访问
```bash
# 检查服务状态
systemctl status dashboard

# 检查端口占用
netstat -tulnp | grep 8050

# 检查日志
tail -100 /root/dashboard/dashboard.log
```

### 数据文件未找到
```bash
# 检查文件是否存在
ls -lh /root/dashboard/reports/

# 检查权限
chmod -R 755 /root/dashboard/
```

---

## 🎉 部署完成后

✅ 访问地址：`http://你的服务器IP:8050`  
✅ 24小时稳定运行  
✅ 同事可以随时访问  
✅ 不依赖你的电脑开机状态  

**下一步优化**（可选）：
- 配置域名（如 `dashboard.yourcompany.com`）
- 添加HTTPS加密
- 设置访问密码
- 配置自动备份
